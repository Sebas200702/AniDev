---
import { getTagColor } from '@utils'
interface Props {
  tag: string
  type?: string
  style?: string
}
const { tag, type, style } = Astro.props

const tagColor = getTagColor(type ?? '')
---

<a
  href=""
  class={`${style ? style : 'w-min'} text-xs h-min font-medium  text-gray-800 px-2 py-1 transition-all duration-200 ease-in-out rounded-2xl hover: ${tagColor}`}
>
  {tag}
</a>
