import type { AnimeSong, ArtistInfo } from 'types'

import { useEffect } from 'react'
import { useFetch } from '@hooks/useFetch'

export const Artist = ({ name, id }: { name: string; id: string }) => {

  
}
