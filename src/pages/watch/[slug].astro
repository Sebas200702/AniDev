---
import { WatchAnime } from '@components/watch-anime/watch-anime'
import Layout from '@layouts/base.astro'
const slug = Astro.params.slug ?? ''
const currentEpisode = parseInt(Astro.params.episode ?? '')
---

<Layout>
  <WatchAnime client:load slug={slug} />
</Layout>
