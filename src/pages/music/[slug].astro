---
import { Music } from '@components/music-info/music'
import Layout from '@layouts/base.astro'

const { slug } = Astro.params
const [_, theme_id] = slug?.split('_') ?? ['', '']
---

<Layout themeId={theme_id}>
  <Music client:load themeId={theme_id} />
</Layout>
