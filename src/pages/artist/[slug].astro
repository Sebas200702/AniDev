---
import { ArtistInfoContainer } from '@artist/components/artist-info/artist-info-container'
import Layout from '@layouts/base.astro'
const { slug } = Astro.params
const [name] = slug?.split('_') ?? ['']
---

<Layout artistName={name}>
  <ArtistInfoContainer client:load name={name} />
</Layout>
