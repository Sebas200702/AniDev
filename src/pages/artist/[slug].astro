---
import Layout from '@layouts/base.astro'
const { slug } = Astro.params
const [name, id] = slug?.split('_') ?? ['', '']
import { ArtistComponent } from '@components/artist/artist'
---

<Layout>
  <ArtistComponent client:load artistId={id} name={name} />
</Layout>
