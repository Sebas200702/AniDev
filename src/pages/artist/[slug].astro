---
import { ArtistInfo } from '@components/artist/ArtistInfo'
import Layout from '@layouts/base.astro'
const { slug } = Astro.params
const [name] = slug?.split('_') ?? ['']
---

<Layout>
  <ArtistInfo client:load name={name} />
</Layout>
