---
import { CharacterContainer } from '@character/components/character-details/character-container'
import Layout from '@layouts/base.astro'

const slug = Astro.params.slug ?? ''
const characterId = slug.split('_')[1]
---

<Layout characterId={characterId}>
  <CharacterContainer client:load slug={slug} />
</Layout>
